<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
  <h3>Comments</h3>
  <ul>
    {{#each comments}}
    <li>{{this.text}} - {{format_date this.date_created}}</li>
    {{/each}}
  </ul>
  <form "/project/{{id}}/comment" method="POST">
    <textarea class="comment-text" name="comment" rows="3" placeholder="Add a comment"></textarea>
    <button class="comment-button" type="submit" class="btn btn-primary">Add Comment</button>
  </form>
</div>